{
    "scripts": {
        "build": "graphql-codegen && tsc && cd ./built && zip -r getJokes_function.zip *"
    },
    "devDependencies": {
        "@graphql-codegen/cli": "^2.2.0",
        "@graphql-codegen/typescript": "^2.2.2",
        "@types/aws-lambda": "^8.10.83",
        "graphql": "^15.6.0",
        "typescript": "^4.4.3"
    }
}
